#include <stdio.h>

int main() {
    // Declare an array of integers
    int numbers[] = {1, 2, 3, 4, 5};

    // Display elements of the array
    for (int i = 0; i < 5; ++i) {
        printf("%d ", numbers[i]);
    }

    return 0;
}
